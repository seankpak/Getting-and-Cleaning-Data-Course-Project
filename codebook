finaldata.R script performs the following actions as decribed in the course project's definition.

1. Download the dataset and extract in UCI HAR Dataset folder
2. Assign variables to each data
3. Merge the training and the test sets to create on data set
4. Extract only the measurements on the mean and standard deviation for each measurement
5. Use descriptive activity names to anme the activities in the dataset
6. Appropriately lable the dataset with descriptive variable names 
7. From the dataset in step 4, create a second independent tidy dataset with the average of each variable for each activity and each subject
